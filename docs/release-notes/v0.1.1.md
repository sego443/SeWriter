# SeWriter v0.1.1

- Fixed selection-delete undo behavior in the editor.
- After selecting a large range, deleting it, and pressing Cmd+Z, SeWriter restores the text, clears the selection, and places the cursor at the pre-delete active cursor position.
